@import "./styles/iconfont.wxss";

/* 注意：在微信小程序中 不支持 通配符--* 所以需要选中每个需要设置统一样式的标签 */
page,
view,
text,
swiper,
swiper-item,
image,
navigator {
  padding: 0;
  margin: 0;
  box-sizing: border-box;
}

page {
  /* 定义主题颜色 */
  --themeColor: #eb4450;
  font-size: 28rpx;
}
image {
  width: 100%;
}
